##################################################
## Service Accounts — Cross (Build pipelines)
##################################################

# NOTE: The runtime backend service account (sa-mktskills-backend-api)
# is defined in scripts/prod/iam/iam_service_accounts.tf.
# Do not duplicate it here — both stacks target the same GCP project.
